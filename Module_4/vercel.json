{
  "version": 2,
  "installCommand": "npm install && npx prisma generate && npx esbuild api/index.ts --bundle --platform=node --target=node20 --outfile=api/index.js --format=esm",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ]
}
